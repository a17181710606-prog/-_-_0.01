# 本机 PostgreSQL 连接串（服务端专用，不会打进前端包）
# 服务器上由 deploy/setup.sh 首次运行时自动生成，无需手填
DATABASE_URL=postgresql://equipment:password@127.0.0.1:5432/equipment

# 会话签名密钥（预留；随机 64 位十六进制）
SESSION_SECRET=your-random-secret

# 启用数据库后端（构建期内联）。本地 UI 开发可不设置 → 走种子数据模式
NEXT_PUBLIC_ENABLE_DB=1
